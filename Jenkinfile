pipeline {
    agent any
    stages {
        stage("working with loops") {
            steps {
                script {
                    def a = 1
                    while (a <= 10) {
                        println "Your looped value: ${a}"
                        a = a + 1
                    }
                }
            }
        }
    }
}

